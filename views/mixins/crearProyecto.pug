//- Definición de mixin de nombre crearProyecto y que define un argumento
//- por defecto: un proyecto vacio
mixin crearProyecto(proyecto = {})

    form.agregar-proyecto(action=`/nuevo-proyecto/${proyecto.id || ''}` method="POST")
                .campo 
                    label(for="nombre" ) Nombre proyecto 
                    input(type="text" value=proyecto.nombre id="nombre" name="nombre" placeholder="Nombre Proyecto")

                .campo.enviar 
                    input(type="submit" value=`${proyecto.nombre ? 'Guardar' : 'Agregar'}` class="boton")